# Force platform-specific optional packages to resolve for Linux x64
# This prevents Windows-generated lockfiles from including aix/win/darwin binaries
# that cause EBADPLATFORM errors on Railway's Linux x64 build environment.
os=linux
cpu=x64
